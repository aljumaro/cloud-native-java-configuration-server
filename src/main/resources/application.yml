server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/aljumaro/cloud-native-java-configuration-repository

logging:
  level:
    com.aljumaro.test: DEBUG